'use client';

import { useEffect } from 'react';

// This component is now a no-op as Firestore has been removed.
export function FirebaseErrorListener() {
  useEffect(() => {
    // No-op
  }, []);

  return null;
}
