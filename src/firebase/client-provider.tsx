'use client';

import React, { type ReactNode } from 'react';

// This is a placeholder provider as Firebase has been removed.
export function FirebaseClientProvider({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
